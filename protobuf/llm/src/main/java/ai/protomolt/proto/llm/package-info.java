/**
 * LLM-facing schema annotations ({@code llm.v1}): descriptor options that direct how a
 * model fills a form, plus the reader that surfaces them. Independent of validation,
 * metadata, and quality; renderers (notably protomolt-prompt) consume this package.
 */
package ai.protomolt.proto.llm;

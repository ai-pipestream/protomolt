description = 'Git proto gatherer: clone/fetch repositories and stage proto trees (JGit)'

dependencies {
    api project(':protomolt-gather')
    api project(':protomolt-actions')

    implementation libs.jgit
}

plugins {
    alias(libs.plugins.protobuf)
}

description = 'The parser-fleet document model: the canonical docling-core v2 parity document.proto (fleet repos re-vendor it from here, byte-identical), plus projections between it and the repo document'

dependencies {
    api libs.protobuf.java
    api libs.protobuf.java.util
    // The projection targets the repo document contract.
    api project(':protomolt-repo-proto')
}

// Messages only: the fleet's parse services own the RPC surfaces.
protobuf {
    protoc {
        artifact = "com.google.protobuf:protoc:4.35.1"
    }
}

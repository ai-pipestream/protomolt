plugins {
    alias(libs.plugins.protobuf)
}

description = 'Protobuf indexing standard — descriptor hints, plans, and NDJSON projection'

dependencies {
    api project(':protomolt-search-index-spi')
    api project(':protomolt-search-index-ndjson')
    api project(':protomolt-protobuf-validation')
    api libs.protobuf.java

    // Extract .proto imports for test schemas (hints + validate).
    protobuf project(':protomolt-search-index-spi')
    protobuf project(':protomolt-protobuf-validation')
}

protobuf {
    protoc {
        artifact = "com.google.protobuf:protoc:4.35.1"
    }
}

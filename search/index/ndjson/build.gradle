description = 'Engine-agnostic protobuf → NDJSON encoding'

dependencies {
    api project(':protomolt-descriptors')
    api libs.protobuf.java.util
    // Message bodies use protobuf JsonFormat; the small bulk-action envelope uses Jackson so no
    // JSON is escaped by hand.
    implementation libs.jackson.databind
}

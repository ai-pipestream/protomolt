description = 'Ranked-list equivalence certification for rerank providers serving the same model'

dependencies {
    api project(':protomolt-rerank')

    testImplementation project(':protomolt-rerank-tei')
    testImplementation project(':protomolt-rerank-ovms')
}

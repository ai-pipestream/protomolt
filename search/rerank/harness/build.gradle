description = 'Ranked-list equivalence certification for rerank providers serving the same model'

dependencies {
    api project(':protomolt-search-rerank')

    testImplementation project(':protomolt-search-rerank-tei')
    testImplementation project(':protomolt-search-rerank-ovms')
}

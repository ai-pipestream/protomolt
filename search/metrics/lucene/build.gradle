description = 'Lucene metric executor — aggregate collectors over the doc values the search door already writes'

dependencies {
    api project(':protomolt-metric-spi')
    api project(':protomolt-index-spi')
    api libs.lucene.core
    // The metrics role module: composer types in its signature, the door
    // services behind it, and the search door's store it aggregates over.
    api project(':protomolt-composer')
    implementation project(':protomolt-metric-door')
    implementation project(':protomolt-search-door')
    implementation project(':protomolt-actions')
    // With a co-mounted registry the module registers the rebuild-rollup
    // workflow, the way the search door registers its own.
    implementation project(':protomolt-registry')
    implementation libs.grpc.api
    implementation libs.slf4j.api

    // The composed-role proof dials the fake repo and the metric door over
    // their real wire contracts.
    testImplementation project(':protomolt-repo-proto')
    testImplementation libs.grpc.netty.shaded
    testImplementation libs.grpc.inprocess
    testRuntimeOnly libs.slf4j.simple
}

// The Lucene 11 line ships Java 25 bytecode, so this module's tests cannot run on an
// older JVM: the test launcher stays on the toolchain JDK and -PtestJdk is ignored
// here (same pin as :protomolt-index-lucene).
tasks.withType(Test).configureEach {
    javaLauncher = javaToolchains.launcherFor {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

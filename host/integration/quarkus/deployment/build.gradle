description = 'Quarkus build-time integration for Pipestream protobuf tools'

dependencies {
    implementation platform(libs.quarkus.bom)
    implementation project(':protomolt-integration-quarkus')
    implementation libs.quarkus.arc.deployment
}

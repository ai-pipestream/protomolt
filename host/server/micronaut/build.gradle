description = 'Micronaut host adapter for protobuf JSON/REST'

dependencies {
    api project(':protomolt-server-common')
    // Controllers stay in the consuming Micronaut app; this module is the shared facade.
    compileOnly libs.micronaut.inject
}

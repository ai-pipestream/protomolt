description = 'Vert.x 5 host for protobuf JSON/REST'

dependencies {
    api project(':protomolt-server-common')
    api libs.vertx.core
    api libs.vertx.web

    testImplementation libs.vertx.junit5
    testImplementation libs.vertx.web.client
}

plugins {
    alias(libs.plugins.protobuf)
}

description = 'Descriptor-grounded gRPC recipe, artifact, and run-evidence contracts'

dependencies {
    api libs.protobuf.java
    api project(':protomolt-grpc-service-profile')
    // The recipe contract names inference.v1 Usage, AttemptOutcome, and
    // FinishReason in the structured-generation evidence.
    api project(':protomolt-inference-proto')
}

protobuf {
    protoc {
        artifact = "com.google.protobuf:protoc:4.35.1"
    }
}

description = 'Agent-facing service registration, reflection refresh, and descriptor inspection actions'

dependencies {
    api project(':protomolt-actions')
    api project(':protomolt-grpc-service-profile')

    implementation project(':protomolt-descriptors')
    implementation project(':protomolt-grpc-invoke')
    implementation project(':protomolt-registry')
    implementation libs.protobuf.java.util

    testImplementation libs.grpc.inprocess
    testImplementation libs.grpc.protobuf
    testImplementation libs.grpc.services
}

plugins {
    alias(libs.plugins.protobuf)
}

description = 'Protobuf service profiles and durable descriptor artifacts for the gRPC recipe workbench'

dependencies {
    api libs.protobuf.java
    api libs.protobuf.java.util
}

protobuf {
    protoc {
        artifact = "com.google.protobuf:protoc:4.35.1"
    }
}

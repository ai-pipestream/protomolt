{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "description": "Confluent is boxed to 8.0.x: 8.1+ (Kafka 4.1-based) references org.apache.kafka.common.metrics.Monitorable, absent from the pinned kafka-clients 3.9 line. See the comment in gradle/libs.versions.toml; lift this when kafka-clients moves to >= 4.1.",
      "matchPackagePatterns": ["^io\\.confluent"],
      "allowedVersions": "<8.1"
    }
  ]
}

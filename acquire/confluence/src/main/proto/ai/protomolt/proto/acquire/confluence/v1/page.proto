syntax = "proto3";

package ai.protomolt.proto.acquire.confluence.v1;

import "ai/protomolt/proto/acquire/confluence/v1/common.proto";
import "ai/protomolt/proto/acquire/confluence/v1/label.proto";
import "ai/protomolt/proto/acquire/confluence/v1/operation.proto";
import "ai/protomolt/proto/acquire/confluence/v1/property.proto";
import "ai/protomolt/proto/index/hints/v1/indexing_hints.proto";
import "ai/protomolt/proto/validate/v1/validate.proto";
import "google/protobuf/timestamp.proto";

option java_multiple_files = true;
option java_package = "ai.protomolt.proto.acquire.confluence.v1";

// ============================================================================
// PAGE
// ============================================================================

// A page: the primary unit of Confluence content. Labels, properties,
// operations, likes and versions are plain repeated fields (a list is
// `repeated`; pagination is transport and is not modeled).
message Page {
  // ID of the page.
  string id = 1 [
    (ai.protomolt.proto.index.hints.v1.index) = { type: INDEX_FIELD_TYPE_KEYWORD },
    (ai.protomolt.proto.validate.v1.field) = { required: true }
  ];
  // The status of the page.
  ContentStatus status = 2 [
    (ai.protomolt.proto.index.hints.v1.index) = { type: INDEX_FIELD_TYPE_KEYWORD }
  ];
  // Title of the page.
  string title = 3 [
    (ai.protomolt.proto.index.hints.v1.index) = {
      type: INDEX_FIELD_TYPE_TEXT
      analyzer: "english"
      sub_fields: [{ type: INDEX_FIELD_TYPE_KEYWORD, name: "raw" }]
    }
  ];
  // ID of the space the page is in.
  string space_id = 4 [
    (ai.protomolt.proto.index.hints.v1.index) = { type: INDEX_FIELD_TYPE_KEYWORD },
    (ai.protomolt.proto.validate.v1.field) = { required: true }
  ];
  // ID of the parent page, or null if there is no parent page.
  string parent_id = 5;
  // The type of the parent page, or null if there is no parent page.
  ParentContentType parent_type = 6;
  // Position of the page within the given parent page tree.
  optional int32 position = 7;
  // The account ID of the user who created this page originally.
  string author_id = 8 [
    (ai.protomolt.proto.index.hints.v1.index) = { type: INDEX_FIELD_TYPE_KEYWORD }
  ];
  // The account ID of the user who owns this page.
  optional string owner_id = 9;
  // The account ID of the user who owned this page previously, or null if
  // there is no previous owner.
  optional string last_owner_id = 10;
  // The subtype of the page.
  optional string subtype = 11;
  // Date and time when the page was created.
  google.protobuf.Timestamp created_at = 12 [
    (ai.protomolt.proto.index.hints.v1.index) = { type: INDEX_FIELD_TYPE_DATE }
  ];
  // The current version of the page.
  Version version = 13;
  // The body of the page, in the format(s) requested by the fetch.
  Body body = 14;
  // The labels attached to the page.
  repeated Label labels = 15;
  // The content properties attached to the page.
  repeated ContentProperty properties = 16;
  // The operations the calling user is permitted to perform on the page.
  repeated Operation operations = 17;
  // The likes recorded on the page.
  repeated Like likes = 18;
  // The version history of the page.
  repeated Version versions = 19;
  // Whether the page has been favorited by the current user.
  bool is_favorited_by_current_user = 20;
  // Human-facing URL of the content, for search results and UIs.
  string web_url = 21;
  // Extensible key-value metadata (extension-metadata map - see convention).
  map<string, string> metadata = 99;
}

description = 'Bill of materials for the Pipestream protobuf tools suite'

// One supported publication inventory drives both the Maven BOM and the consumer-facing
// Gradle catalog. Keeping it here prevents those two version-alignment surfaces from drifting.
def supportedModules = [
        'protomolt-descriptors',
        'protomolt-shapes',
        'protomolt-workflow',
        'protomolt-helpers',
        'protomolt-formats',
        'protomolt-sources',
        'protomolt-acquire-gather',
        'protomolt-acquire-gather-git',
        'protomolt-acquire-gather-maven',
        'protomolt-emit',
        'protomolt-emit-okf',
        'protomolt-emit-parquet',
        'protomolt-emit-parquet-s3',
        'protomolt-acquire-msgraph',
        'protomolt-acquire-confluence',
        'protomolt-acquire-pull',
        'protomolt-acquire-s3',
        'protomolt-acquire-jdbc',
        'protomolt-iceberg',
        'protomolt-iceberg-s3',
        'protomolt-compat',
        'protomolt-actions',
        'protomolt-registry',
        'protomolt-registry-proto',
        'protomolt-mcp',
        'protomolt-grpc-channel-policy',
        'protomolt-grpc-invoke',
        'protomolt-grpc-service-contract',
        'protomolt-grpc-service-profile',
        'protomolt-grpc-service-workspace',
        'protomolt-grpc-workflow',
        'protomolt-grpc-service',
        'protomolt-grpc-validation',
        'protomolt-grpc-validation-micrometer',
        'protomolt-serve',
        'protomolt-cli',
        'protomolt-agent-host',
        'protomolt-acp',
        'protomolt-acp-agent',
        'protomolt-acquire-connector',
        'protomolt-kafka-connect',
        'protomolt-kafka-connect-iceberg',
        'protomolt-kafka-connect-opensearch',
        'protomolt-kafka-wire',
        'protomolt-kafka-serde',
        'protomolt-kafka-serde-micrometer',
        'protomolt-codegen',
        'protomolt-registry-service',
        'protomolt-http-jsonschema',
        'protomolt-protobuf-validation-protovalidate',
        'protomolt-mapper',
        'protomolt-mapper-cel',
        'protomolt-projection',
        'protomolt-pipeline',
        'protomolt-delegation',
        'protomolt-screening',
        'protomolt-receipt',
        'protomolt-record-verifier',
        'protomolt-authz',
        'protomolt-authz-grpc',
        'protomolt-authz-jdbc',
        'protomolt-mesh-proto',
        'protomolt-mesh-contracts',
        'protomolt-mesh-cluster',
        'protomolt-mesh-runtime',
        'protomolt-schema-apicurio',
        'protomolt-schema-apicurio-deployment',
        'protomolt-schema-confluent',
        'protomolt-mapper-metadata',
        'protomolt-protobuf-metadata',
        'protomolt-protobuf-quality',
        'protomolt-protobuf-llm',
        'protomolt-protobuf-prompt',
        'protomolt-protobuf-validation',
        'protomolt-protobuf-types',
        'protomolt-protobuf-seo',
        'protomolt-search-index-spi',
        'protomolt-search-chunk',
        'protomolt-search-proto',
        'protomolt-protobuf-metric',
        'protomolt-metric-proto',
        'protomolt-metric-spi',
        'protomolt-metric-lucene',
        'protomolt-metric-service',
        'protomolt-metric-iceberg',
        'protomolt-search-service',
        'protomolt-search-snapshot-s3',
        'protomolt-search-console',
        'protomolt-search-index-ndjson',
        'protomolt-search-index-lucene',
        'protomolt-search-index-opensearch',
        'protomolt-search-index-solr',
        'protomolt-search-index-qdrant',
        'protomolt-search-index-protobuf',
        'protomolt-search-embedding',
        'protomolt-search-embedding-model2vec',
        'protomolt-search-embedding-tei',
        'protomolt-search-embedding-ovms',
        'protomolt-search-embedding-harness',
        'protomolt-search-rerank',
        'protomolt-search-rerank-tei',
        'protomolt-search-rerank-ovms',
        'protomolt-search-rerank-harness',
        'protomolt-integration-quarkus',
        'protomolt-integration-quarkus-deployment',
        'protomolt-integration-spring',
        'protomolt-composer',
        'protomolt-config',
        'protomolt-config-registry',
        'protomolt-config-kafka',
        'protomolt-server-common',
        'protomolt-server-jdk',
        'protomolt-server-vertx',
        'protomolt-server-netty',
        'protomolt-server-spring',
        'protomolt-server-micronaut',
        'protomolt-server-quarkus',
        'protomolt-http-json',
        'protomolt-http-rest',
        'protomolt-http-openapi',
        'protomolt-asset-proto',
        'protomolt-asset-characterize',
        'protomolt-repo-proto',
        'protomolt-repo-container',
        'protomolt-repo-service',
        'protomolt-account-proto',
        'protomolt-account-service',
        'protomolt-jobs-proto',
        'protomolt-jobs-service',
        'protomolt-inference-proto',
        'protomolt-inference-spi',
        'protomolt-inference-openvino',
        'protomolt-inference-service',
        'protomolt-inference-openai',
        'protomolt-inference-structured',
        'protomolt-intake-proto',
        'protomolt-intake-service',
        'protomolt-parse-proto',
        'protomolt-parse-document',
        'protomolt-parse-service',
        'protomolt-parse-text',
        'protomolt-parse-grparse',
        'protomolt-parse-playground',
        'protomolt-document-platform',
]

javaPlatform {
    allowDependencies()
}

catalog {
    versionCatalog {
        version('protomolt', project.version.toString())
        supportedModules.each { moduleName ->
            String alias = moduleName.substring('protomolt-'.length())
            library(alias, project.group.toString(), moduleName).versionRef('protomolt')
        }
        bundle('core', ['descriptors', 'helpers', 'sources'])
        bundle('schema-governance', ['compat', 'registry', 'registry-service'])
        bundle('validation', ['protobuf-validation',
                              'protobuf-validation-protovalidate'])
        bundle('mapping', ['mapper', 'mapper-cel', 'mapper-metadata', 'projection'])
        bundle('dynamic-runtime', ['actions', 'grpc-channel-policy', 'grpc-invoke', 'grpc-service-contract',
                                   'grpc-service-profile', 'grpc-service-workspace', 'grpc-workflow',
                                   'grpc-service', 'serve'])
        bundle('search', ['search-index-spi', 'search-index-ndjson', 'search-index-lucene',
                          'search-index-opensearch', 'search-index-solr',
                          'search-index-qdrant', 'search-index-protobuf'])
    }
}

dependencies {
    constraints {
        supportedModules.each { moduleName ->
            api project(":${moduleName}")
        }
    }
}

// Modules that publish but are deliberately NOT constrained by the BOM. Everything else
// published must appear above - checkBomCompleteness fails the build otherwise, so a new
// module cannot be omitted by accident.
def deliberatelyUnconstrained = [
        'bom',                                          // the platform itself
        'samples', 'protomolt-system-tests',            // never published
        'protomolt-protobuf-validation-conformance',    // conformance executor, not a library
]

tasks.register('checkBomCompleteness') {
    description = 'Fails when a published module is missing from the BOM constraints'
    group = 'verification'
    def moduleNames = rootProject.subprojects.collect { it.name }
    inputs.property('supportedModules', supportedModules)
    doLast {
        def constrained = configurations.api.dependencyConstraints.collect { it.name } as Set
        def missing = moduleNames.findAll {
            !deliberatelyUnconstrained.contains(it) && !constrained.contains(it)
        }
        if (!missing.isEmpty()) {
            throw new GradleException("Published modules missing from the BOM: ${missing}. "
                    + "Constrain them in bom/build.gradle or add them to "
                    + "deliberatelyUnconstrained with a reason.")
        }
    }
}

tasks.register('checkConsumerCatalog') {
    description = 'Checks that the published version catalog represents ProtoMolt consumers'
    group = 'verification'
    dependsOn tasks.named('generateCatalogAsToml')
    def generatedCatalog = layout.buildDirectory.file('version-catalog/libs.versions.toml')
    inputs.file(generatedCatalog)
    doLast {
        String text = generatedCatalog.get().asFile.text
        supportedModules.each { moduleName ->
            if (!text.contains("name = \"${moduleName}\"")) {
                throw new GradleException(
                        "Consumer version catalog is missing ${moduleName}")
            }
        }
        if (text.contains('org.junit') || text.contains('axion-release')) {
            throw new GradleException(
                    'Consumer version catalog leaked ProtoMolt build dependencies')
        }
    }
}

tasks.named('check') {
    dependsOn tasks.named('checkBomCompleteness')
    dependsOn tasks.named('checkConsumerCatalog')
}

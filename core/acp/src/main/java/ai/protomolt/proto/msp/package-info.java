/**
 * A client for the Muse Session Protocol (MSP), the JSON-RPC session host Muse Code serves
 * over stdio with {@code muse serve}. It shares the newline-delimited JSON-RPC framing with
 * the ACP client and adds the MSP session and turn lifecycle an unattended driver needs.
 */
package ai.protomolt.proto.msp;
